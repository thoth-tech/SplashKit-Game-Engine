#pragma once

#include "splashkit.h"

#include <cstdint>
#include <string>
using std::string;

// struct color
// {
//     float r, g, b, a;
// };

// struct point_2d
// {
//     double x, y;
// };

// struct vector_2d
// {
//     double x, y;
// };

// // Define a bitmap struct
// typedef struct _bitmap_data *bitmap;

// class window
// {
// public:
//     ~window() {}

//     // Open the window with specified dimensions and caption
//     void open(const std::string &caption, int width, int height);

//     // Close the window
//     void close();

//     // Check if the window is open
//     bool isOpen() const;

//     // Other potential methods related to window operations...
// };
